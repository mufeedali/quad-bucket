# Personal Access Token for the bot account on Forgejo
# Scopes required: repo (read/write), user (read), issue (read/write), organization (read)
RENOVATE_TOKEN=

# Personal Access Token for fetching release notes from GitHub.com
# This is optional but highly recommended to avoid rate limits and get changelogs.
# Scopes required: public_repo (read-only)
RENOVATE_GITHUB_COM_TOKEN=
